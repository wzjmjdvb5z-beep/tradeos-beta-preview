{
  "name": "tradeos-mobile",
  "private": true,
  "version": "0.1.0",
  "description": "TradeOS iOS shell and mobile build tooling",
  "scripts": {
    "mobile:prepare": "node scripts/prepare-mobile.mjs",
    "ios:generate": "npm run mobile:prepare && npx cap add ios",
    "ios:sync": "npm run mobile:prepare && npx cap sync ios"
  },
  "dependencies": {
    "@capacitor/app": "^8.0.0",
    "@capacitor/browser": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "@capacitor/haptics": "^8.0.0",
    "@capacitor/ios": "^8.0.0",
    "@capacitor/share": "^8.0.0",
    "@capacitor/splash-screen": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0",
    "esbuild": "^0.25.0"
  }
}
