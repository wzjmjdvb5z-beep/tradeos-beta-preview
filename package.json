{
  "name": "veystead-mobile",
  "private": true,
  "version": "0.1.0",
  "description": "Veystead iOS shell and mobile build tooling",
  "scripts": {
    "mobile:prepare": "node scripts/prepare-mobile.mjs",
    "ios:generate": "npm run mobile:prepare && npx cap add ios && node scripts/install-native-ios.mjs",
    "ios:sync": "npm run mobile:prepare && npx cap sync ios && node scripts/install-native-ios.mjs"
  },
  "dependencies": {
    "@capacitor/app": "^8.0.0",
    "@capacitor/browser": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "@capacitor/haptics": "^8.0.0",
    "@capacitor/ios": "^8.0.0",
    "@capacitor/share": "^8.0.0",
    "@capacitor/splash-screen": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0",
    "esbuild": "^0.25.0"
  }
}
